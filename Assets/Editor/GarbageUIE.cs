using UnityEditor.UIElements;
using UnityEngine.UIElements;
using UnityEditor;

//[CustomPropertyDrawer(typeof(datastuff))]
public class GarbageUIE : PropertyDrawer
{
    /*
    public override VisualElement CreatePropertyGUI(SerializedProperty property)
    {
        VisualElement container = new VisualElement();
        var garbej = new PropertyField(property.FindPropertyRelative("Number"));

        container.Add(garbej);
        return container;
    }
    */

}
